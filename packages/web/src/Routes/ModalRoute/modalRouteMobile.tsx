import {Route} from 'react-router';
import React from 'react';

import {EnumRouteSlugs} from 'src/Routes/EnumRouteSlugs';

export const modalRouteMobile = (
	<Route path={EnumRouteSlugs.MODAL}>
	</Route>
);
